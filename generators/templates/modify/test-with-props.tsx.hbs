import React from 'react'
import { render, screen, RenderResult } from '@testing-library/react'

import { {{pascalCase name}}Props } from '.'

type SutTypes = {
sut: RenderResult
}

const makeSut = (props?: Partial<{{pascalCase name}}Props>): SutTypes => {
const sut = render(<{{pascalCase name}} {...props}/>)

return {
sut
}
}

describe('{{pascalCase name}}', () => {
test('Should render {{pascalCase name}} correctly', () => {
makeSut()

expect(screen.getByText(/{{pascalCase name}}/i)).toBeInTheDocument()
})
})
