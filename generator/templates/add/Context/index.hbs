import React, { createContext, useContext } from 'react'

import {
  {{pascalCase name}}ContextProps,
  {{pascalCase name}}ProviderProps,
  AnyState,
  AnyStateProps,
  {{pascalCase name}}DefaultValues
} from './utils'

/**
 *
 * * Criando o contexto *
 *
 */

export const {{pascalCase name}}Context = createContext<{{pascalCase name}}ContextProps>(
  {{pascalCase name}}DefaultValues
)

/**
 *
 * * --- --- --- --- Criando o contexto --- --- --- --- *
 *
 */

/**
 *
 * * Criando o provider *
 *
 */

const {{pascalCase name}}Provider = ({
  children
}: {{pascalCase name}}ProviderProps) => {
  const [anyState, setAnyState] = React.useState<AnyStateProps>(AnyState)

  const verifyIfIsLoading = () => anyState.isLoading

  return (
    <{{pascalCase name}}Context.Provider value=\{{
      anyState,
      setAnyState,
      verifyIfIsLoading
    }}>
      {children}
    </{{pascalCase name}}Context.Provider>
  )
}

/**
 *
 * * --- --- --- --- Criando o provider --- --- --- --- *
 *
 */

/**
 *
 * * criando o contexto *
 *
 */

const Use{{pascalCase name}}Context = () => useContext({{pascalCase name}}Context)

/**
 *
 * * --- --- --- --- criando o contexto --- --- --- --- *
 *
 */

export {
  {{pascalCase name}}Provider, Use{{pascalCase name}}Context
}