import React from 'react'

import styles from 'components/layout/components/footer/styles.module.scss'

export const Footer = () => {
  return (
    <footer data-testid="main-footer" className={`${styles.container} `}>
      Footer
    </footer>
  )
}